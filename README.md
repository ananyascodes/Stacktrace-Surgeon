# Stacktrace-Surgeon
A tool that operates inside the terminal, with direct filesystem and git access, can close the loop autonomously for a meaningful subset of failures — without requiring the developer to leave their workflow. 
